{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Git list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Author</th>
                <th>Date</th>
                <th>Repositoryowner</th>
                <th>Repository</th>
                <th>Comment</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('git_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.author }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.repositoryOwner }}</td>
                <td>{{ entity.repository }}</td>
                <td>{{ entity.git }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('git_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('git_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('git_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
